#!/bin/bash
sed -i "s/  const handleMachineChange = (selectedName: string) => {/  const handleDepartmentChange = (dept: string) => {\n    setDepartment(dept);\n    if (NON_MACHINE_DEPTS.includes(dept)) {\n      setMachineName('');\n      setMachineNo('');\n      setValidationError(null);\n    } else {\n      const matched = machinesList.find(m => (m[4] || m[0]) === machineName);\n      if (matched \&\& matched[1] !== dept) {\n        setMachineName('');\n        setMachineNo('');\n        setValidationError(null);\n      }\n    }\n  };\n\n  const handleMachineChange = (selectedName: string) => {/" src/components/breakdown/UserBreakdownEntry.tsx
