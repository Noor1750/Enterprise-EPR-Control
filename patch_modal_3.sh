#!/bin/bash
sed -i "s/  const handleMachineChange = (selectedName: string) => {/  const handleDepartmentChange = (dept: string) => {\n    setDepartment(dept);\n    if (NON_MACHINE_DEPTS.includes(dept)) {\n      setMachineName('');\n      setMachineNo('');\n      setValidationError(null);\n    } else {\n      const matched = availableMachines.find(m => m.name === machineName);\n      if (matched \&\& matched.department !== dept) {\n        setMachineName('');\n        setMachineNo('');\n        setValidationError(null);\n      }\n    }\n  };\n\n  const handleMachineChange = (selectedName: string) => {/" src/components/breakdown/BreakdownModal.tsx
